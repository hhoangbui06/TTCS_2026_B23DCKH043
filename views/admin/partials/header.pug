header.header
    .container-fluid
        .row.align-items-center
            .col-3
                .inner-logo
                    a(href="/admin/dashboard") ADMIN
            .col-9
              
              div(class="text-right")
                a(class="btn btn-primary btn-sm mr-2" style="color:white; display:inline; margin-right:10px"
                  href=`${prefixAdmin}/my-account`
                  
                ) #{user.fullName}
                a(
                  href=`${prefixAdmin}/auth/logout`
                  class="btn btn-danger btn-sm"
                ) Đăng xuất
              
