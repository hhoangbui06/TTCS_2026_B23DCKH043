const express=require('express')
const router=express.Router()
const systemConfig=require('../../config/system')
const uploadCloud=require('../../middlewares/admin/fileupload-middleware')
const multer=require('multer')
const upload=multer()
const validate=require('../../validate/admin/account-validate')
const controller=require('../../controllers/admin/my-account-controller')
module.exports=router.get('/', controller.index)
module.exports=router.get('/edit', upload.single('avatar'), uploadCloud.upload, controller.edit)
module.exports=router.patch('/edit', upload.single('avatar'),uploadCloud.upload,validate.patchAccount, controller.patchAccount)